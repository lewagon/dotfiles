def color_of_heroku(specs)
  sentence = specs || "purple79589"

  # TODO: return the non-digit characters of the sentence

end

def color_of_postgresql(specs)
  sentence = specs || "somewhatbetweenblueandindigo"

  # TODO: return the last 6 characters of the sentence

end

def color_of_css3(specs)
  sentence = specs || %({
  Roses are red,
  Violets are blue,
  Sugar is sweet,
  And so are you.
  })

  # TODO: return the following word:
  # - it contains exactly 4 letters
  # - it's located between a space and a comma

end

def color_of_rake(specs)
  sentence = specs || "red or green?"

  # TODO: find the word located before a question mark

end

def color_of_javascript(specs)
  sentence = specs || "black letters on a yellow background"

  # TODO: find the word containing two consecutive "l"

end

def color_of_html5(specs)
  sentence = specs || %({
  The new logo of Firefox has evolved to a more iconized style.
  The color scheme stays the same, with red, orange and yellow shades.
  })

  # TODO: return the following word:
  # - it contains exactly 6 letters
  # - it's composed of letters from "a" to "r"

end

def color_of_ruby(specs)
  sentence = specs || %({
  Ruby is a dynamic open source programming language.
  Elegant syntax, easy to write...Definitely a progammer's best friend.
  })

  # TODO: capture all the capital letters in the sentence
  # return a string formed with the letters you found
  # (Code Hint: what is more suitable between `match` and `scan`?)
  
end