require 'rake'
require 'rake/testtask'

require 'minitest/autorun'
require 'minitest/pride'

task :default do
  describe "index.html" do
    it "should changed some HTML" do
      (File.open("index.html","r").readlines.size == 75).must_equal false
    end
  end
end