const assert = require("assert");

describe("Semantics", () => {
  it("should return 'even' for 0", () => {
    assert.equal(document.querySelector('h1').textContent, 'The POUR Principles');
  });
});
