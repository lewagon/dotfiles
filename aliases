# Lewagon Aliases
# Get External IP / Internet Speed
alias myip="curl https://ipinfo.io/json" # or /ip for plain-text ip
alias speedtest="curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -"

# Quickly serve the current directory as HTTP
alias serve='ruby -run -e httpd . -p 8000' # Or python -m SimpleHTTPServer :)

# NOTE: On Q3 2021, Le Wagon decided to change the Web Dev curriculum default text editor
alias stt="echo 'Launching VS Code instead of Sublime Text... (cf ~/.aliases)' && code ."


# Theos Aliases
# Terminal aliases
alias la="ls -a"
alias cl="clear"
alias al="nvim /Users/theodrak/.aliases"

# --- Le Wagon updates
alias cdl="cd /Users/theodrak/code/theodrak/fullstack-challenges"
alias cde="cd /Users/theodrak/code/theodrak/exercises"
alias cdp="cd /Users/theodrak/code/theodrak/teamprojects/crypto-tracker"
alias cdt="cd /Users/theodrak/code/theodrak/teamprojects"
alias cda="cd ~/code/theodrak/sass/aspiron"
alias cdf="cd ~/code/theodrak/sass/sass_rails_8_book"
alias cdr="cd ~/code/theodrak/rails-investigations"
alias lwu="git pull upstream master -X ours"

# --- Git Shortcuts
alias gip="git push"
alias gic="git commit -m"
alias gis="git status"
alias gia="git add ."

alias ghrpu="gh repo create --source=. --public"
alias ghrpr="gh repo create --source=. --private"
alias giinit="git init -b main"
alias lg="LazyGit"

# ---- Eza (better ls) -----
alias ls="eza --icons=always"


# ---- Zoxide (better cd) ----
eval "$(zoxide init zsh)"
alias cd="z"

alias rails="bin/rails"
